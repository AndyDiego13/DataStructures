#include <iostream>
#include <map>
#include <vector>
#include "Libro.hpp"

//esto es para el reto
std::map< std::string, int> conexionesPorDia(std::string fecha)
{
    //Creamos el mapa
    std::map< std::string, int> conexiones;

    //procesamiento

    //Se regresa el mapa
    return conexiones;
}

int main()
{
    std::map< int, std::string, std::string, std::vector<Libro>> registros;

    registros = (1,"Maria", "5", Libro("5","El señor de los anillos", 700);

    /* Mapa con una estructura lineal */
    // std::map< std::string, std::vector<Libro>> combinado;

    //Para el reto
    std::map< std::string, int> m = conexionesPorDia("10-11-2021");

    return 0;
}
